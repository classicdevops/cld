if echo "$TEMPLATE" | grep --quiet "C7WP" ; then
 KVM_CREATE=$C7WP_CREATE
 TEMPLATE_CONFIGURE=$C7WP_CONFIGURATION
 TEMPLATE_AFTER_START='qm set $VMID -delete ide2'
elif echo "$TEMPLATE" | grep --quiet "C7SNT" ; then
 KVM_CREATE=$C7SNT_CREATE
 TEMPLATE_CONFIGURE=$C7SNT_CONFIGURATION
 TEMPLATE_AFTER_START='qm set $VMID -delete ide2'
elif echo "$TEMPLATE" | grep --quiet "C7MNCRFT" ; then
 KVM_CREATE=$C7MNCRFT_CREATE
 TEMPLATE_CONFIGURE=$C7MNCRFT_CONFIGURATION
 TEMPLATE_AFTER_START='qm set $VMID -delete ide2'
elif echo "$TEMPLATE" | grep --quiet "C7GTLB" ; then
 KVM_CREATE=$C7GTLB_CREATE
 TEMPLATE_CONFIGURE=$C7GTLB_CONFIGURATION
 TEMPLATE_AFTER_START='qm set $VMID -delete ide2'
elif echo "$TEMPLATE" | grep --quiet "C7YII2" ; then
 KVM_CREATE=$C7YII2_CREATE
 TEMPLATE_CONFIGURE=$C7YII2_CONFIGURATION
 TEMPLATE_AFTER_START='qm set $VMID -delete ide2'
elif echo "$TEMPLATE" | grep --quiet "C7WBMN" ; then
 KVM_CREATE=$C7WBMN_CREATE
 TEMPLATE_CONFIGURE=$C7WBMN_CONFIGURATION
 TEMPLATE_AFTER_START='qm set $VMID -delete ide2'
elif echo "$TEMPLATE" | grep --quiet "U18JTSMT" ; then
 KVM_CREATE=$U18JTSMT_CREATE
 TEMPLATE_CONFIGURE=$U18JTSMT_CONFIGURATION
 TEMPLATE_AFTER_START='qm set $VMID -delete ide2'
elif echo "$TEMPLATE" | grep --quiet "C7CWP" ; then
 KVM_CREATE=$C7CWP_CREATE
 TEMPLATE_CONFIGURE=$C7CWP_CONFIGURATION
 TEMPLATE_AFTER_START='qm set $VMID -delete ide2'
fi

