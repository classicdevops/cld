ionice -c3 mongodump --forceTableScan --host ${MONGO_HOST} -u "${MONGO_USER}" -p "${MONGO_PASSWORD}" --authenticationDatabase "admin" --gzip --out ${SERVER_BACKUP_DIR}/${INSTANCE}/${DATE}/${BACKUP_METHOD}/
